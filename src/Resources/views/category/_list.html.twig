{%- if categories|length > 0 -%}
    <nav class="lyssal-blog-categories">
        <ul>
            {%- for category in categories -%}
                <li>
                    {{ appellation_html(category) }}
                </li>
            {%- endfor -%}
        </ul>
        <nav class="lyssal-blog-categories__end"></nav>
    </nav>
{%- endif -%}
